<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="nativescript-image-cache" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>

        <Button text="nativescript-searchable-select" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-sewoo-printer" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-woosim-printer" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-google-maps" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-intermec-printer" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-socketmobile" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-t3b-printer" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-x-printer-bt" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>